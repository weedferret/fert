/**
 * This is the template for creating commands. All commands should be named after their default initiator "!8ball" should be named "8ball", for example.
 * Commands are to be placed in the /commands/ directory where they will be loaded on startup.
 * Copy this code into a new file within the /commands/ directory and write some magic!
 */
exports.run = async (client, message, args, level) => {
}

exports.conf = {
	name: 'COMMANDNAME',
	aliases: []	//	Array of aliases for the command.
};